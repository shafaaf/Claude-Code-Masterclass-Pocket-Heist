rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Test mode: Allow all reads and writes (development only)
    // TODO: Update these rules for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
